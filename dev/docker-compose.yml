version: '3.7'
services:
  api:
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - 5000:5000
  nginx:
    build:
      context: nginx/
      dockerfile: Dockerfile
    ports:
      - 80:80
      - 443:443
